import ReactSlider from 'react-slider';
import React from 'react';

const Slider = () => {
  return (
    <ReactSlider className="customSlider" trackClassName="customSlider-track" />
  );
};

export default Slider;
